syntax = "proto3";

package timeline.v1;

option go_package = "timeline/v1";

/**
 * TimelineService is the service that provides access 
 * to home user timeline for other services. 
 * It is assumed that consumers pass the userID 
 * when making requests on their behalf 
 * in the context metadata (key: x-user).
 */
service TimelineService {
}